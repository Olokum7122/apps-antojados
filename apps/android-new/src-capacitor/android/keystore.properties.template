# Copia este archivo como keystore.properties (mismo directorio) y rellena los valores.
# keystore.properties NO se commitea al repo (esta en .gitignore).
# Guarda el archivo .jks en un lugar seguro fuera del repo o en esta carpeta (tambien en .gitignore).

storeFile=../../antojadosmx-release.jks
storePassword=TU_STORE_PASSWORD
keyAlias=antojadosmx
keyPassword=TU_KEY_PASSWORD
